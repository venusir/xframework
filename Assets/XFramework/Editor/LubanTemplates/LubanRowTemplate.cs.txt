// ============================================================
// XFramework Luban Row Template
// ============================================================
// 此文件是 Luban 生成代码的模板参考。
// 使用 Luban 工具链生成代码后，确保每个 Row 类型实现 IConfigRow。
//
// Luban 命令示例（在 luban.conf 所在目录执行）：
//   luban --template_dir Templates --output_code_dir ../Generated
//
// 生成后检查每个 TbXxx.cs 中的 Row 定义是否包含：
//   public int Id { get; set; }
// 并实现 XFramework.XConfig.IConfigRow 接口。
// ============================================================

using XFramework.XConfig;

namespace {{Namespace}}
{
    /// <summary>
    /// Luban 生成的配置行示例。
    /// <para>必须实现 <see cref="IConfigRow"/>，提供 <c>Id</c> 属性作为主键。</para>
    /// </summary>
    [System.Serializable]
    public partial struct {{RowName}} : IConfigRow
    {
        /// <summary>主键。</summary>
        public int Id { get; set; }

        // Luban 生成的字段（由工具自动填充）
        // 例如：public string Name;
        //       public int Value;
        //       public float Rate;
    }
}